use std.runtime;
use std.print.printf;

fun fibr(n: i64) i64 {
    if (n < 2) { ret n; }
    ret fibr(n - 1) + fibr(n - 2);
}

#[symbol("main")]
fun main(argc: i64, argv: **u8) i64 {
    printf("Hello from Mach!\n");
    printf("fibr(10) = {}\n", fibr(10));

    ret 0;
}
